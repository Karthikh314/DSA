from typing import List

class Solution:
    def largestElement(self, n:int,arr : List[int]):
        max = arr[0]
        for i in range (1,n):
           if arr[i]>max:
              max=arr[i]
        return max

        

